# docker-compose.prod.yml
version: "3.8"

services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile.prod
    restart: always
    env_file:
      - ./api/.env
    ports:
      - "5000:5000"
    volumes:
      - ./api:/app
      - /app/node_modules
    networks:
      - appesso-network

  nginx:
    build:
      context: .
      dockerfile: nginx.Dockerfile
    restart: always
    depends_on:
      - api
    ports:
      - "80:80"
      - "5555:5555"
    networks:
      - appesso-network

  studio:
    build:
      context: ./api
    restart: always
    depends_on:
      - api # Keep dependency on api
    command: npx prisma studio --hostname 0.0.0.0 --port 5555
    env_file:
      - ./api/.env
    networks:
      - appesso-network

networks:
  appesso-network:
    driver: bridge

volumes:
  db-data:
